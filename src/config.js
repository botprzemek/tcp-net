const config = {
    quarter: {
        amount: 4,
        length: 600000,
    }
};

export const getQuarterAmount = () => config.quarter.amount;

export const getQuarterLength = () => config.quarter.length;